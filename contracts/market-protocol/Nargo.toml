[package]
name = "market_protocol"
type = "lib"
authors = [""]
compiler_version = ">=0.18.0"

[dependencies]
protocol_types = { git = "https://github.com/AztecProtocol/aztec-packages", tag = "v5.0.1", directory = "noir-projects/noir-protocol-circuits/crates/types" }
